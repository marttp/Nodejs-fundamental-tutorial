const add = (op1, op2) => op1 + op2;
const minus = (op1, op2) => op1 - op2;
const multiply = (op1, op2) => op1 * op2;
const divide = (op1, op2) => op1 / op2;

module.exports = {
    add,
    minus,
    multiply,
    divide
}